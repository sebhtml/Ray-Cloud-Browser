Get-Sub-Graph-With-Path
	This tool create the javascript for a path.

	The graph file kmers.txt is generated with Ray -write-kmers

	The path file is a fasta file containing the sequence.

	The generated Javascript file must be added into the 
	development deployment of Ray Cloud Browser

-- Sébastien Boisvert

