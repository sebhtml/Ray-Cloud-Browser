- readahead is not good enough (padding is pad)
- MouseDown intefere with acceleration when pressing movie buttons
- movie controls don't move
- push currentVertex in PathOperator upon selection
- don't show any vertices below the minimum
- user can change the minimum coverage to display

- add buttons to go to minimum and maximum
- select part of graph to obtain DNA sequence
- delete off-screen objects
- display edge if one end is on screens

- server -- return {} when there is an error instead of empty content
- server -- return array instead of {} for vertices
